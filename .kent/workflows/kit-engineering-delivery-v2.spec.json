{
  "name": "Kit Engineering Delivery v2",
  "description": "Plan, implement, independently verify, fix, and deliver an engineering change for kent-engineering-kit.",
  "execution_target": "default-branch",
  "nodes": [
    {
      "key": "backlog",
      "kind": "start",
      "display_name": "Backlog",
      "agent": null,
      "completion_mode": null,
      "script_path": null
    },
    {
      "key": "plan",
      "kind": "agent",
      "display_name": "Plan",
      "agent": "default",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "plan_review",
      "kind": "agent",
      "display_name": "Independent Plan Review",
      "agent": "spec-reviewer",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "plan_contract",
      "kind": "script",
      "display_name": "Plan Contract Accept",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-plan-contract-accept"
    },
    {
      "key": "plan_contract_continue",
      "kind": "script",
      "display_name": "Plan Contract Continue Guard",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-plan-contract-continue"
    },
    {
      "key": "plan_contract_verify",
      "kind": "script",
      "display_name": "Plan Contract Verify Guard",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-plan-contract-verify"
    },
    {
      "key": "plan_revalidation",
      "kind": "agent",
      "display_name": "Plan Revalidation",
      "agent": "default",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "implement",
      "kind": "agent",
      "display_name": "Implement",
      "agent": "implementation-worker",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "verification_dispatch",
      "kind": "script",
      "display_name": "Verification Dispatch",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-verification-dispatch"
    },
    {
      "key": "deterministic_verify",
      "kind": "script",
      "display_name": "Deterministic Verify",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-verify-report"
    },
    {
      "key": "verification_gate",
      "kind": "agent",
      "display_name": "Verification Gate",
      "agent": "workflow-gate",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "fix",
      "kind": "agent",
      "display_name": "Fix",
      "agent": "fix-worker",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "cleanup",
      "kind": "agent",
      "display_name": "Cleanup",
      "agent": "delivery-operator",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "wont_do",
      "kind": "terminal",
      "display_name": "Won't Do",
      "agent": null,
      "completion_mode": null,
      "script_path": null
    },
    {
      "key": "done",
      "kind": "terminal",
      "display_name": "Done",
      "agent": null,
      "completion_mode": null,
      "script_path": null
    },
    {
      "key": "standards_review",
      "kind": "agent",
      "display_name": "Standards Review",
      "agent": "standards-reviewer",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "verification_join",
      "kind": "join",
      "display_name": "Verification Join",
      "agent": null,
      "completion_mode": null,
      "script_path": null
    },
    {
      "key": "prepare_pr",
      "kind": "agent",
      "display_name": "Prepare PR",
      "agent": "delivery-operator",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "waiting_pr",
      "kind": "agent",
      "display_name": "Waiting PR",
      "agent": "ci-monitor",
      "completion_mode": "shell_command",
      "script_path": null
    },
    {
      "key": "merge_watch",
      "kind": "script",
      "display_name": "Wait For PR Merge",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-wait-github-pr"
    },
    {
      "key": "task_janitor",
      "kind": "script",
      "display_name": "Task Janitor",
      "agent": null,
      "completion_mode": null,
      "script_path": ".kent/scripts/workflow-task-janitor"
    }
  ],
  "edges": [
    {
      "key": "start_plan",
      "source": "backlog",
      "transition": "backlog_start",
      "target": "plan",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Plan {{.TaskShortId}}: {{.TaskTitle}}\n\nRead `.kent/context/plan.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan`, `evidence_type` to\n`plan`, and `context.manifest_path` to `.kent/context/plan.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nSelect exactly one supported `work_kind` before detailed planning:\n\n- `feature` — Add approved Kit workflow or tooling behavior; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `bugfix` — Restore intended behavior for a reproducible Kit defect; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `refactor` — Improve structure without intended behavior changes; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `migration` — Migrate approved source contracts in independently verified slices; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `dependency` — Audit or update approved tooling dependencies; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `test` — Improve deterministic verification without product expansion; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n\nA standalone `work_kind: <key>` declaration in the task body is an explicit\nhuman routing decision and wins when the key is supported. Otherwise classify\nconservatively from the requested outcome and source context. Do not use the\nJira issue type alone as proof. If the kind is unsupported, multiple kinds are\nmaterially coupled, or the classification remains ambiguous, complete with\n`plan_needs_user_action` and ask for one supported key.\n\nAfter selection, load the mapped Plan procedure as a procedure module. Some\nproject procedures also document execution; this Plan node must use only their\nbootstrap, discovery, design/specification, analysis, planning, and plan-review\nsections. The generated stage contract overrides any instruction to edit\nproduction files, execute plan steps, invoke nested prompt flows, or launch a\nseparate delivery workflow.\n\n\n\nTask body:\n{{.TaskBody}}\n\nKeep discovery, design/spec ingestion, decisions, and implementation planning in\nthis one Plan session. Ask questions when a product decision is required. Do not\ninvoke nested prompt flows and do not implement production changes.\nBefore planning implementation, write an explicit scope boundary containing the\nincluded root source IDs, related evidence, dependencies, and deferred or\nout-of-scope issues. A relationship, shared parent, common design file, or\nadjacent implementation does not merge issue scopes.\n\nInventory dependency and cross-module impact before implementation. SDK or\nschema upgrades, generated-contract changes, and adaptations in modules outside\nthe root issue must be identified as either a required dependency adaptation or\na separate deferred task. Do not silently widen product behavior to make a\ndependency compile. Preserve generated enum, sealed, identifier, provider,\nstatus, action, and flow types through domain boundaries; free-form string\nrouting requires evidence that no typed source contract exists plus an explicit\nunknown-value strategy.\n\nAssign every required evidence artifact an owner and a production-edit\nboundary. If the plan requires a pre-edit red run, make it the first\nwriter-owned step and require capture before any production edit. The plan must\nnot turn a later failure to capture agent-owned evidence into a user approval.\nAny design, specification, or plan that narrows, replaces, or claims to\nsupersede the task body must cite the exact human-authored task-comment ID or\nanother explicit authoritative source. Agent-authored comments, implementation\ninference, and unsupported claims that \"the user clarified\" are not product\nauthority. Use `plan_needs_user_action` before implementation when that provenance\nis absent.\n\n\nComplete with `plan_review_plan` only when the plan has no unresolved product, API,\nUX, or safety ambiguity. `workspace_path` is the repository or\nmanaged-worktree root; it is never `.todo/<feature>` or another artifact\ndirectory. Provide that root plus `plan_path`, selected `work_kind`,\n`plan_route=start`, `plan_route_context=not-applicable`,\n`task_short_id={{.TaskShortId}}`, and a concise `review_context` naming the\ngoverning authority, source IDs, acceptance criteria, planned evidence, and\nrisks. Use the literal `not-applicable` as `plan_path` only when the project\ncontract explicitly allows planless work. Kent transition parameters must be\nnon-empty.\nComplete with `plan_needs_user_action` and `blocker_reason` for an external blocker.\nChoose `plan_wont_do` only for an explicit cancellation decision and provide\n`closure_reason`.",
      "transition_description": "Start one planning session for this task.",
      "parameters": [],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_review",
      "source": "plan",
      "transition": "plan_review_plan",
      "target": "plan_review",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Independently review the proposed plan for\n{{.TaskShortId}} without editing files.\n\nRead `.kent/context/review.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan_review`, `evidence_type` to\n`review`, and `context.manifest_path` to `.kent/context/review.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nPlan: {{.Params.plan_path}}\nWork kind: {{.Params.work_kind}}\nRequested post-review route: {{.Params.plan_route}}\nRoute context: {{.Params.plan_route_context}}\nPlanning context: {{.Params.review_context}}\n\nRead the task body, current human-authored comments, exact source records named\nby the plan, and the plan itself. Use the read-only `spec-reviewer` contract,\nadapted to the proposed plan rather than an implementation diff. Do not edit\nthe plan, code, task, or external systems.\n\nCheck:\n\n- every narrowed or superseded decision has exact human authority;\n- root scope is separate from related evidence and deferred work;\n- product, API, UX, architecture, safety, and destructive-action choices are\n  explicit rather than invented by the planner;\n- acceptance criteria map to owned deterministic, review, or runtime evidence;\n- dependencies and generated-contract adaptations stay bounded;\n- writer steps are executable, dependency ordered, and do not hide workflow\n  stages as implementation work.\n\nChoose `plan_review_accepted` only when implementation or verification may safely follow.\nProvide `workspace_path`, `plan_path`, `work_kind`, unchanged `plan_route`,\nunchanged `plan_route_context`, `task_short_id={{.TaskShortId}}`, a concise\n`plan_review_report`, and refreshed `review_context` that includes the review\nresult. `plan_route_context` is `not-applicable` except when preserving the\nremaining bounded Fix bundle for `fix_continue`.\n\nChoose `plan_review_needs_changes` for plan-contract defects and provide the same identity,\nunchanged route and route context, `plan_review_report`, and `review_context`;\nthe retained Plan session will revise the artifact. Choose `plan_review_needs_user_action`\nonly for a real missing product decision or external authority and provide the\npreserved identity/context plus `blocker_reason`. Choose `plan_review_wont_do` only for\nexplicit cancellation and provide `closure_reason`.\nRead `.kent/commands/plan.md` for the project preview-review procedure.\nVerify the first independent read-only PASS receipt: it must identify a\ndifferent reviewer Session and bind the exact preview SHA-256 now reviewed.\nPerform the second independent review of that same preview hash. Revalidation\nrequires a refreshed first review and this separate second review.\nOnly after both PASS may plan_review_accept request human approval.\nInclude a concise Russian approval summary naming scope and effects; do not\nrequest a third routine preview review or claim the graph hashes receipts.\n",
      "transition_description": "Planning is complete; independently review its authority, scope, evidence, and executable ordering.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_review_accept",
      "source": "plan_review",
      "transition": "plan_review_accepted",
      "target": "plan_contract",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "The independent plan review passed; accept the normalized plan contract and continue through its declared route.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_review_report",
          "description": "Independent read-only review of plan authority, scope, evidence, and ordering.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_review_revalidate",
      "source": "plan_review",
      "transition": "plan_review_needs_changes",
      "target": "plan_revalidation",
      "context": "continue_session",
      "context_source": "node:plan",
      "prompt": "Revalidate the authoritative plan for {{.TaskShortId}}.\n\nRead `.kent/context/plan.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan_revalidation`, `evidence_type` to\n`plan`, and `context.manifest_path` to `.kent/context/plan.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nPlan: {{.Params.plan_path}}\nWork kind: {{.Params.work_kind}}\nIntended route after acceptance: {{.Params.plan_route}}\nRoute context: {{.Params.plan_route_context}}\nCurrent context: {{.Params.review_context}}\nIndependent Plan Review findings: {{.Params.plan_review_report}}\n\nContinue the retained planning context. Re-read current task comments and exact\nauthority sources. Distinguish operational feedback that fits the accepted\ncontract from material changes to requirements, architecture, acceptance,\nsafety, or evidence. Reconcile only material changes in the authoritative\ndesign/specification/plan and cite exact human-authored task-comment IDs or\nother explicit sources. Do not edit production code or execute verification.\nWhen revalidation was triggered by deterministic drift, compare the current\nplan with the prior normalized snapshot at\n`.kent/runtime/{{.TaskShortId}}/plan-contract.json`; checkbox state alone\nis intentionally absent from that contract.\n\nChoose `plan_revalidation_review_plan` after reconciliation and provide `workspace_path`,\n`plan_path`, unchanged `work_kind`, unchanged `plan_route`,\nunchanged `plan_route_context`, `task_short_id={{.TaskShortId}}`, and\nrefreshed `review_context`. Preserve the remaining bounded Fix bundle only in\n`plan_route_context` when the route is `fix_continue`; otherwise use\n`not-applicable`. The independent Plan Review will run again before the new\nnormalized snapshot is accepted. Use `plan_revalidation_needs_user_action` with preserved\ncontext and `blocker_reason` only for a real unresolved decision or external\nauthority. Choose `plan_revalidation_wont_do` only for explicit cancellation and provide\n`closure_reason`.",
      "transition_description": "The read-only review found plan-contract defects; revise the plan before any writer or verification stage proceeds.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_review_report",
          "description": "Independent read-only review of plan authority, scope, evidence, and ordering.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_review_needs_user_action",
      "source": "plan_review",
      "transition": "plan_review_needs_user_action",
      "target": "plan_review",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `plan_review` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `plan_review_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nIf the latest user instruction explicitly cancels the task, choose `plan_review_wont_do` and provide `closure_reason`.\nRead `.kent/commands/plan.md` for the project preview-review procedure.\nVerify the first independent read-only PASS receipt: it must identify a\ndifferent reviewer Session and bind the exact preview SHA-256 now reviewed.\nPerform the second independent review of that same preview hash. Revalidation\nrequires a refreshed first review and this separate second review.\nOnly after both PASS may plan_review_accept request human approval.\nInclude a concise Russian approval summary naming scope and effects; do not\nrequest a third routine preview review or claim the graph hashes receipts.\n",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        },
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_review_wont_do",
      "source": "plan_review",
      "transition": "plan_review_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_revalidation_review",
      "source": "plan_revalidation",
      "transition": "plan_revalidation_review_plan",
      "target": "plan_review",
      "context": "continue_session",
      "context_source": "previous_target",
      "prompt": "Independently review the proposed plan for\n{{.TaskShortId}} without editing files.\n\nRead `.kent/context/review.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan_review`, `evidence_type` to\n`review`, and `context.manifest_path` to `.kent/context/review.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nPlan: {{.Params.plan_path}}\nWork kind: {{.Params.work_kind}}\nRequested post-review route: {{.Params.plan_route}}\nRoute context: {{.Params.plan_route_context}}\nPlanning context: {{.Params.review_context}}\n\nRead the task body, current human-authored comments, exact source records named\nby the plan, and the plan itself. Use the read-only `spec-reviewer` contract,\nadapted to the proposed plan rather than an implementation diff. Do not edit\nthe plan, code, task, or external systems.\n\nCheck:\n\n- every narrowed or superseded decision has exact human authority;\n- root scope is separate from related evidence and deferred work;\n- product, API, UX, architecture, safety, and destructive-action choices are\n  explicit rather than invented by the planner;\n- acceptance criteria map to owned deterministic, review, or runtime evidence;\n- dependencies and generated-contract adaptations stay bounded;\n- writer steps are executable, dependency ordered, and do not hide workflow\n  stages as implementation work.\n\nChoose `plan_review_accepted` only when implementation or verification may safely follow.\nProvide `workspace_path`, `plan_path`, `work_kind`, unchanged `plan_route`,\nunchanged `plan_route_context`, `task_short_id={{.TaskShortId}}`, a concise\n`plan_review_report`, and refreshed `review_context` that includes the review\nresult. `plan_route_context` is `not-applicable` except when preserving the\nremaining bounded Fix bundle for `fix_continue`.\n\nChoose `plan_review_needs_changes` for plan-contract defects and provide the same identity,\nunchanged route and route context, `plan_review_report`, and `review_context`;\nthe retained Plan session will revise the artifact. Choose `plan_review_needs_user_action`\nonly for a real missing product decision or external authority and provide the\npreserved identity/context plus `blocker_reason`. Choose `plan_review_wont_do` only for\nexplicit cancellation and provide `closure_reason`.\nRead `.kent/commands/plan.md` for the project preview-review procedure.\nVerify the first independent read-only PASS receipt: it must identify a\ndifferent reviewer Session and bind the exact preview SHA-256 now reviewed.\nPerform the second independent review of that same preview hash. Revalidation\nrequires a refreshed first review and this separate second review.\nOnly after both PASS may plan_review_accept request human approval.\nInclude a concise Russian approval summary naming scope and effects; do not\nrequest a third routine preview review or claim the graph hashes receipts.\n",
      "transition_description": "The plan contract was reconciled; independently re-review it before accepting the new snapshot.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_revalidation_needs_user_action",
      "source": "plan_revalidation",
      "transition": "plan_revalidation_needs_user_action",
      "target": "plan_revalidation",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `plan_revalidation` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `plan_revalidation_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nIf the latest user instruction explicitly cancels the task, choose `plan_revalidation_wont_do` and provide `closure_reason`.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        },
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_revalidation_wont_do",
      "source": "plan_revalidation",
      "transition": "plan_revalidation_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_contract_continue_revalidate",
      "source": "plan_contract_continue",
      "transition": "plan_contract_continue_changed",
      "target": "plan_revalidation",
      "context": "continue_session",
      "context_source": "node:plan",
      "prompt": "Revalidate the authoritative plan for {{.TaskShortId}}.\n\nRead `.kent/context/plan.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan_revalidation`, `evidence_type` to\n`plan`, and `context.manifest_path` to `.kent/context/plan.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nPlan: {{.Params.plan_path}}\nWork kind: {{.Params.work_kind}}\nIntended route after acceptance: {{.Params.plan_route}}\nRoute context: {{.Params.plan_route_context}}\nCurrent context: {{.Params.review_context}}\nDetected plan-contract change: {{.Params.plan_change_report}}\n\nContinue the retained planning context. Re-read current task comments and exact\nauthority sources. Distinguish operational feedback that fits the accepted\ncontract from material changes to requirements, architecture, acceptance,\nsafety, or evidence. Reconcile only material changes in the authoritative\ndesign/specification/plan and cite exact human-authored task-comment IDs or\nother explicit sources. Do not edit production code or execute verification.\nWhen revalidation was triggered by deterministic drift, compare the current\nplan with the prior normalized snapshot at\n`.kent/runtime/{{.TaskShortId}}/plan-contract.json`; checkbox state alone\nis intentionally absent from that contract.\n\nChoose `plan_revalidation_review_plan` after reconciliation and provide `workspace_path`,\n`plan_path`, unchanged `work_kind`, unchanged `plan_route`,\nunchanged `plan_route_context`, `task_short_id={{.TaskShortId}}`, and\nrefreshed `review_context`. Preserve the remaining bounded Fix bundle only in\n`plan_route_context` when the route is `fix_continue`; otherwise use\n`not-applicable`. The independent Plan Review will run again before the new\nnormalized snapshot is accepted. Use `plan_revalidation_needs_user_action` with preserved\ncontext and `blocker_reason` only for a real unresolved decision or external\nauthority. Choose `plan_revalidation_wont_do` only for explicit cancellation and provide\n`closure_reason`.",
      "transition_description": "The normalized accepted plan changed materially; reconcile authority and acceptance before continuing.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_change_report",
          "description": "Deterministic summary of material plan-contract changes.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_verify_revalidate",
      "source": "plan_contract_verify",
      "transition": "plan_contract_verify_changed",
      "target": "plan_revalidation",
      "context": "continue_session",
      "context_source": "node:plan",
      "prompt": "Revalidate the authoritative plan for {{.TaskShortId}}.\n\nRead `.kent/context/plan.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `plan_revalidation`, `evidence_type` to\n`plan`, and `context.manifest_path` to `.kent/context/plan.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nPlan: {{.Params.plan_path}}\nWork kind: {{.Params.work_kind}}\nIntended route after acceptance: {{.Params.plan_route}}\nRoute context: {{.Params.plan_route_context}}\nCurrent context: {{.Params.review_context}}\nDetected plan-contract change: {{.Params.plan_change_report}}\n\nContinue the retained planning context. Re-read current task comments and exact\nauthority sources. Distinguish operational feedback that fits the accepted\ncontract from material changes to requirements, architecture, acceptance,\nsafety, or evidence. Reconcile only material changes in the authoritative\ndesign/specification/plan and cite exact human-authored task-comment IDs or\nother explicit sources. Do not edit production code or execute verification.\nWhen revalidation was triggered by deterministic drift, compare the current\nplan with the prior normalized snapshot at\n`.kent/runtime/{{.TaskShortId}}/plan-contract.json`; checkbox state alone\nis intentionally absent from that contract.\n\nChoose `plan_revalidation_review_plan` after reconciliation and provide `workspace_path`,\n`plan_path`, unchanged `work_kind`, unchanged `plan_route`,\nunchanged `plan_route_context`, `task_short_id={{.TaskShortId}}`, and\nrefreshed `review_context`. Preserve the remaining bounded Fix bundle only in\n`plan_route_context` when the route is `fix_continue`; otherwise use\n`not-applicable`. The independent Plan Review will run again before the new\nnormalized snapshot is accepted. Use `plan_revalidation_needs_user_action` with preserved\ncontext and `blocker_reason` only for a real unresolved decision or external\nauthority. Choose `plan_revalidation_wont_do` only for explicit cancellation and provide\n`closure_reason`.",
      "transition_description": "The normalized accepted plan changed materially; reconcile authority and acceptance before verification.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route",
          "description": "Post-review route: start, continue, verify, or fix_continue.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_route_context",
          "description": "Route-specific context; use not-applicable unless continuing a Fix bundle.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_change_report",
          "description": "Deterministic summary of material plan-contract changes.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_implement",
      "source": "plan_contract",
      "transition": "plan_contract_start",
      "target": "implement",
      "context": "compact_and_continue_session",
      "context_source": "node:plan",
      "prompt": "Implement {{.TaskShortId}}: {{.TaskTitle}}\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `implement`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPlan: {{.Params.plan_path}}. Workspace: {{.Params.workspace_path}}.\n\nSelected work kind: `{{.Params.work_kind}}`.\n\nUse the matching Implement procedure from this profile catalog:\n\n- `feature` — Add approved Kit workflow or tooling behavior; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `bugfix` — Restore intended behavior for a reproducible Kit defect; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `refactor` — Improve structure without intended behavior changes; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `migration` — Migrate approved source contracts in independently verified slices; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `dependency` — Audit or update approved tooling dependencies; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `test` — Improve deterministic verification without product expansion; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n\nThe selected key must exist in the catalog and match the authoritative plan. If\nit does not, stop with `implement_needs_user_action`; do not silently reclassify it. Load\nonly the mapped Implement procedure. When that file combines planning and\nexecution, skip its discovery, planning, audit-launch, and scope-confirmation\nstages: execute exactly one already-approved unchecked plan step.\n\nUse the project procedure for step selection, recipes, editing, focused checks,\nand plan progress. This generated workflow's completion contract overrides any\nlegacy procedure transition names such as `audit`.\n\n\nApply the `implementation-worker` role contract to exactly one ready\nwriter-owned plan step. Capture any planned pre-edit evidence before the first\nproduction edit.\n\nAfter marking that step complete, choose `implement_continue_implementation` with\n`workspace_path`, `task_short_id={{.TaskShortId}}`, and a concise\n`review_context` with the completed step, changed files, checks, and next ready\nstep when unchecked writer-owned ready steps remain. Choose `implement_verify` when every\nwriter-owned plan step is complete; provide the same workspace/task identity\nplus `review_context` summarizing plan/spec paths, the fixed comparison point,\nchanged files, checks, risks, and any downstream runtime acceptance scope.\nBoth outcomes pass through graph-owned deterministic Plan Contract checks;\nwriter output cannot select accept mode or a different continuation route.\nUse `implement_needs_user_action` only for an external blocker and provide\n`blocker_reason` plus the unchanged `work_kind`. Its approval is a resume signal\nafter the named external action is complete, not acknowledgement of waiting;\nstate that condition explicitly. Choose `implement_wont_do` only for explicit\ncancellation and provide `closure_reason`.",
      "transition_description": "The accepted plan contract is stable and implementation can start without branch-identity routing.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_continue_implement",
      "source": "plan_contract",
      "transition": "plan_contract_continue",
      "target": "implement",
      "context": "continue_session",
      "context_source": "previous_target_or_new",
      "prompt": "Implement {{.TaskShortId}}: {{.TaskTitle}}\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `implement`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPlan: {{.Params.plan_path}}. Workspace: {{.Params.workspace_path}}.\n\nSelected work kind: `{{.Params.work_kind}}`.\n\nUse the matching Implement procedure from this profile catalog:\n\n- `feature` — Add approved Kit workflow or tooling behavior; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `bugfix` — Restore intended behavior for a reproducible Kit defect; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `refactor` — Improve structure without intended behavior changes; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `migration` — Migrate approved source contracts in independently verified slices; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `dependency` — Audit or update approved tooling dependencies; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `test` — Improve deterministic verification without product expansion; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n\nThe selected key must exist in the catalog and match the authoritative plan. If\nit does not, stop with `implement_needs_user_action`; do not silently reclassify it. Load\nonly the mapped Implement procedure. When that file combines planning and\nexecution, skip its discovery, planning, audit-launch, and scope-confirmation\nstages: execute exactly one already-approved unchecked plan step.\n\nUse the project procedure for step selection, recipes, editing, focused checks,\nand plan progress. This generated workflow's completion contract overrides any\nlegacy procedure transition names such as `audit`.\n\n\nApply the `implementation-worker` role contract to exactly one ready\nwriter-owned plan step. Capture any planned pre-edit evidence before the first\nproduction edit.\n\nAfter marking that step complete, choose `implement_continue_implementation` with\n`workspace_path`, `task_short_id={{.TaskShortId}}`, and a concise\n`review_context` with the completed step, changed files, checks, and next ready\nstep when unchecked writer-owned ready steps remain. Choose `implement_verify` when every\nwriter-owned plan step is complete; provide the same workspace/task identity\nplus `review_context` summarizing plan/spec paths, the fixed comparison point,\nchanged files, checks, risks, and any downstream runtime acceptance scope.\nBoth outcomes pass through graph-owned deterministic Plan Contract checks;\nwriter output cannot select accept mode or a different continuation route.\nUse `implement_needs_user_action` only for an external blocker and provide\n`blocker_reason` plus the unchanged `work_kind`. Its approval is a resume signal\nafter the named external action is complete, not acknowledgement of waiting;\nstate that condition explicitly. Choose `implement_wont_do` only for explicit\ncancellation and provide `closure_reason`.",
      "transition_description": "The accepted plan contract is unchanged; continue with the next ready writer-owned step.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_verify",
      "source": "plan_contract",
      "transition": "plan_contract_verify",
      "target": "verification_dispatch",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "The accepted plan contract is unchanged and writer work is complete; normalize inputs for read-only verification.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_checked_continue",
      "source": "plan_contract_continue",
      "transition": "plan_contract_continue_stable",
      "target": "implement",
      "context": "continue_session",
      "context_source": "previous_target_or_new",
      "prompt": "Implement {{.TaskShortId}}: {{.TaskTitle}}\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `implement`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPlan: {{.Params.plan_path}}. Workspace: {{.Params.workspace_path}}.\n\nSelected work kind: `{{.Params.work_kind}}`.\n\nUse the matching Implement procedure from this profile catalog:\n\n- `feature` — Add approved Kit workflow or tooling behavior; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `bugfix` — Restore intended behavior for a reproducible Kit defect; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `refactor` — Improve structure without intended behavior changes; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `migration` — Migrate approved source contracts in independently verified slices; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `dependency` — Audit or update approved tooling dependencies; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n- `test` — Improve deterministic verification without product expansion; Plan: `.kent/commands/plan.md`; Implement: `.kent/commands/implement.md`.\n\nThe selected key must exist in the catalog and match the authoritative plan. If\nit does not, stop with `implement_needs_user_action`; do not silently reclassify it. Load\nonly the mapped Implement procedure. When that file combines planning and\nexecution, skip its discovery, planning, audit-launch, and scope-confirmation\nstages: execute exactly one already-approved unchecked plan step.\n\nUse the project procedure for step selection, recipes, editing, focused checks,\nand plan progress. This generated workflow's completion contract overrides any\nlegacy procedure transition names such as `audit`.\n\n\nApply the `implementation-worker` role contract to exactly one ready\nwriter-owned plan step. Capture any planned pre-edit evidence before the first\nproduction edit.\n\nAfter marking that step complete, choose `implement_continue_implementation` with\n`workspace_path`, `task_short_id={{.TaskShortId}}`, and a concise\n`review_context` with the completed step, changed files, checks, and next ready\nstep when unchecked writer-owned ready steps remain. Choose `implement_verify` when every\nwriter-owned plan step is complete; provide the same workspace/task identity\nplus `review_context` summarizing plan/spec paths, the fixed comparison point,\nchanged files, checks, risks, and any downstream runtime acceptance scope.\nBoth outcomes pass through graph-owned deterministic Plan Contract checks;\nwriter output cannot select accept mode or a different continuation route.\nUse `implement_needs_user_action` only for an external blocker and provide\n`blocker_reason` plus the unchanged `work_kind`. Its approval is a resume signal\nafter the named external action is complete, not acknowledgement of waiting;\nstate that condition explicitly. Choose `implement_wont_do` only for explicit\ncancellation and provide `closure_reason`.",
      "transition_description": "The accepted plan contract is unchanged; continue with the next ready writer-owned step.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "plan_path",
          "description": "Path to the approved implementation plan, or the literal not-applicable.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_contract_checked_verify",
      "source": "plan_contract_verify",
      "transition": "plan_contract_verify_stable",
      "target": "verification_dispatch",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "The accepted plan contract is unchanged and writer work is complete; normalize inputs for read-only verification.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "plan_needs_user_action",
      "source": "plan",
      "transition": "plan_needs_user_action",
      "target": "plan",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `plan` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `plan_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nIf the latest user instruction explicitly cancels the task, choose `plan_wont_do` and provide `closure_reason`.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "plan_wont_do",
      "source": "plan",
      "transition": "plan_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "implement_continue",
      "source": "implement",
      "transition": "implement_continue_implementation",
      "target": "plan_contract_continue",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "One plan step is complete; check the accepted plan contract before starting the next writer slice.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "implement_verify",
      "source": "implement",
      "transition": "implement_verify",
      "target": "plan_contract_verify",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Implementation is complete; check the accepted plan contract before read-only verification.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "implement_needs_user_action",
      "source": "implement",
      "transition": "implement_needs_user_action",
      "target": "implement",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `implement` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `implement_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nPreserve the selected `{{.Params.work_kind}}` routing key. Use its\nmapped Implement procedure from `.kent/workflow-profile.toml`; do not\nreclassify the task during recovery. Re-emit `work_kind` on\n`implement_continue_implementation` or another `implement_needs_user_action` transition.\nIf the latest user instruction explicitly cancels the task, choose `implement_wont_do` and provide `closure_reason`.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        },
        {
          "key": "work_kind",
          "description": "Stable project-profile work kind selected during Plan.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "implement_wont_do",
      "source": "implement",
      "transition": "implement_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "fix_verify",
      "source": "fix",
      "transition": "fix_verify",
      "target": "plan_contract_verify",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Task-scoped fixes are complete; check the accepted plan contract before rerunning every verification branch.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "fix_needs_user_action",
      "source": "fix",
      "transition": "fix_needs_user_action",
      "target": "fix",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `fix` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `fix_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nIf the latest user instruction explicitly cancels the task, choose `fix_wont_do` and provide `closure_reason`.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "fix_wont_do",
      "source": "fix",
      "transition": "fix_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "dispatch_deterministic_verify",
      "source": "verification_dispatch",
      "transition": "verification_dispatch_fanout_verify",
      "target": "deterministic_verify",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Run deterministic verification and independent read-only reviews.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "dispatch_invalid_workspace",
      "source": "verification_dispatch",
      "transition": "verification_dispatch_invalid_workspace",
      "target": "fix",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Correct invalid workflow metadata without editing production files.\n\nVerification dispatch rejected the reported workspace:\n{{.Params.reported_workspace_path}}\nReason: {{.Params.fix_context}}\n\nTreat Kent's current task execution root as authoritative. Resolve its canonical\nrepository root with `git rev-parse --show-toplevel`, or canonical current\ndirectory for an intentional non-Git workspace. Do not move the worktree,\nchange task artifacts, or edit code.\n\nComplete with `fix_verify` and provide the canonical root as `workspace_path`,\n`task_short_id={{.TaskShortId}}`, plus the preserved `review_context`. Use\n`fix_needs_user_action` only if Kent's execution root itself is unavailable or\nambiguous. Choose `fix_wont_do` only for explicit cancellation and provide\n`closure_reason`.",
      "transition_description": "Reject an artifact or foreign workspace path before verification.",
      "parameters": [
        {
          "key": "reported_workspace_path",
          "description": "Rejected workspace value emitted by an upstream writer.",
          "purpose": "ordinary"
        },
        {
          "key": "fix_context",
          "description": "Deduplicated, dependency-ordered task-scoped repair bundle for the single writer.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "dispatch_standards_review",
      "source": "verification_dispatch",
      "transition": "verification_dispatch_fanout_verify",
      "target": "standards_review",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Run the independent read-only Standards Review.\n\nRead `.kent/context/review.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `standards_review`, `evidence_type` to\n`review`, and `context.manifest_path` to `.kent/context/review.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}\nReview context: {{.Params.review_context}}\n\nApply the `standards-reviewer` role contract to the supplied task delta and its\npinned baseline. Do not edit files. Findings are data for Join, not a routing\ndecision.\n\nComplete only with `standards_review_reported`. Provide `standards_status` as exactly `passed`,\n`needs_changes`, or `blocked`, plus `standards_report` with rule, path, and\ndifferential evidence.",
      "transition_description": "Run deterministic verification and independent read-only reviews.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "deterministic_report_join",
      "source": "deterministic_verify",
      "transition": "deterministic_verify_reported",
      "target": "verification_join",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Deterministic verification completed and reported its status.",
      "parameters": [
        {
          "key": "verification_status",
          "description": "Deterministic verification status: passed, needs_changes, or blocked.",
          "purpose": "ordinary"
        },
        {
          "key": "verification_report",
          "description": "Deterministic verification command, result, and report or log location.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "verification_join_gate",
      "source": "verification_join",
      "transition": "verification_join_evaluate",
      "target": "verification_gate",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Evaluate the joined verification reports without editing files.\n\nRead `.kent/context/review.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `verification_gate`, `evidence_type` to\n`review`, and `context.manifest_path` to `.kent/context/review.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.verification_dispatch_fanout_verify.workspace_path}}\nReview context: {{.Params.verification_dispatch_fanout_verify.review_context}}\nVerification status: {{.Params.verification_status}}\nVerification report: {{.Params.verification_report}}\nStandards status: {{.Params.standards_status}}\nStandards report: {{.Params.standards_report}}\nSpecification review: not enabled by the project profile.\n\nApply the `workflow-gate` role contract. Choose a delivery transition only when\nevery enabled status is `passed`. Do not poll or classify PR CI here; delivery\nowns CI after PR preparation. Keep full reports in `review_context` and make\nuser-facing commentary name only a real decision or external action.\n\nFor `verification_gate_needs_changes`, emit the deduplicated, dependency-ordered repair bundle\nrequired by the `workflow-gate` role contract.\n\nProvide `workspace_path`, a refreshed `review_context` summarizing all reports,\nand the required Smoke decision fields. The refreshed `review_context` must\nrecord the profile Smoke policy, selected transition, rationale, and required\nscope or concrete evidence for bypassing Smoke.\n\nProject policy disables runtime Smoke for this workflow.\nChoose `verification_gate_delivery_ready` and set `smoke_rationale` to the profile policy. Do not\ninvent a Smoke requirement that the project cannot execute.\n\nChoose `verification_gate_needs_changes` for task-scoped failures and provide `workspace_path`\nplus `fix_context`. Choose `verification_gate_needs_user_action` for external or contradictory\nblockers and provide `workspace_path`, `review_context`, and `blocker_reason`;\nafter approval every verification branch reruns. Choose `verification_gate_wont_do` only for an\nexplicit cancellation decision and provide `closure_reason`.",
      "transition_description": "All direct verification branches reported; evaluate them.",
      "parameters": [],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "standards_report_join",
      "source": "standards_review",
      "transition": "standards_review_reported",
      "target": "verification_join",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Repository standards review completed and reported its status.",
      "parameters": [
        {
          "key": "standards_status",
          "description": "Repository standards status: passed, needs_changes, or blocked.",
          "purpose": "ordinary"
        },
        {
          "key": "standards_report",
          "description": "Read-only repository standards, architecture, and engineering report.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "gate_fix",
      "source": "verification_gate",
      "transition": "verification_gate_needs_changes",
      "target": "fix",
      "context": "compact_and_continue_session",
      "context_source": "previous_target_or_new",
      "prompt": "Apply task-scoped fixes for {{.TaskShortId}}.\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `fix`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}. Findings:\n{{.Params.fix_context}}\n\nUse .kent/commands/fix.md as the project procedure.\n\n\nMaintain the ignored `fix` checkpoint with\n`.kent/scripts/workflow-checkpoint <validate|read|write> --stage fix --task\n{{.TaskShortId}} --workspace <workspace>`. Reconcile an existing checkpoint\nbefore repeating work. Pipe one JSON object to `write` before the first\nexpensive or mutating action, after each bounded stage, and before transition.\nIt contains `completed`, `remaining`, and `mutation_ledger` arrays plus one\nnon-empty `next_action`. Record the pinned baseline, supplied findings, completed fix slices, fresh green checks, remaining findings, mutation ledger, and one next permitted action. A same-node `continue_fix` action is consumed when the task re-enters Fix. If the current `fix_context` still contains findings, do not repeat that transition or append bookkeeping-only evidence: rewrite `next_action` to one concrete remaining slice and complete it in the current session. Exclude credentials, authenticated\ncontent, raw logs, and broad device data.\n\nApply the `fix-worker` role contract only to concrete task-introduced or\ntask-worsened findings in `fix_context`. Baseline-wide debt, an unproven\ndifferential, or contradictory policy is not writer scope.\n\nTreat the incoming findings as one dependency-ordered repair bundle. Deduplicate\noverlap, group findings by root cause, and resolve every compatible group in\nthis retained Fix session. Verify each group narrowly and update the checkpoint\nafter meaningful work; do not transition merely to hand off bookkeeping. When\nthe bundle is empty, complete with `fix_verify` and provide `workspace_path`,\n`task_short_id={{.TaskShortId}}`, plus refreshed `review_context` containing\nthe findings, fixes, changed files, artifact paths, and focused checks.\nUse `fix_needs_user_action` only for an external blocker and provide\n`blocker_reason`. Its approval is a resume signal after the named external\naction is complete, not acknowledgement of waiting; state that condition\nexplicitly. Choose `fix_wont_do` only for explicit cancellation and provide\n`closure_reason`.",
      "transition_description": "Verification found task-scoped issues for the single writer to fix.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "fix_context",
          "description": "Deduplicated, dependency-ordered task-scoped repair bundle for the single writer.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "gate_reverify_after_user_action",
      "source": "verification_gate",
      "transition": "verification_gate_needs_user_action",
      "target": "verification_dispatch",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Verification is externally blocked; rerun all branches after approval.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "verification_gate_wont_do",
      "source": "verification_gate",
      "transition": "verification_gate_wont_do",
      "target": "wont_do",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cancel this task only after explicit user approval and record why.",
      "parameters": [
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "gate_delivery_ready",
      "source": "verification_gate",
      "transition": "verification_gate_delivery_ready",
      "target": "prepare_pr",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Prepare delivery for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `prepare_pr`, `evidence_type` to\n`delivery`, and `context.manifest_path` to `.kent/context/delivery.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}. Review context:\n{{.Params.review_context}}\n\nFinal Compliance Review is disabled by the project profile.\n\nUse .kent/commands/ship-pr.md as the project procedure.\n\nThis workflow explicitly authorizes committing the task changes, pushing only\nthe current task branch, and creating or updating its pull request. It never\nauthorizes merging, pushing protected branches, or broadening scope.\n\nTreat `git branch --show-current` as branch authority. The branch may differ\nfrom the Kent task short ID when the project enabled deterministic branch\nidentity; never reconstruct or reject it by comparing branch text with\n`{{.TaskShortId}}`.\n\nIf the task source URL or body identifies an issue in the same repository and\nthis task fully resolves it, include the provider-native closing reference in\nthe pull-request body, such as `Fixes #N` on GitHub. Use a non-closing link for\ncross-repository, partial, or follow-up relationships. Read the task's current\n`source_url` through Kent task metadata when it is not already visible in the\nprompt; do not infer issue linkage from branch text alone.\n\nConfigured PR merge policy: `rebase`.\n\nApply the `delivery-operator` role contract. Resolve the strategy once before\npublishing. On GitHub, serialize repository capabilities, target branch\nprotection, applicable rulesets, merge queue, and PR method state, then run:\n\n`~/.kent/bin/kent-resolve-github-merge-strategy --policy rebase`\n\nThe adapter's structured result is authoritative for strategy selection.\nContinue only when it returns `outcome=resolved`; for\n`outcome=needs_user_action`, preserve its code, candidates, and reason instead\nof manually choosing. Validate the resolved method through the role's\nmethod-specific contract before and after creating or updating the PR.\n\nBefore committing, prove that the checkout is on a task-owned branch permitted\nby the project contract. A source workspace, detached checkout, protected\nbranch, or ambiguous branch owner must route to `prepare_pr_needs_user_action`; never push\nthrough that ambiguity.\n\nComplete through `prepare_pr_monitor_ci` and provide `workspace_path`, `pr_url`, and\n`branch_name`, plus the resolved `merge_strategy`.\n\nIf no PR is genuinely\napplicable, choose `prepare_pr_no_pr` and provide `pr_report`; this path requires\napproval. Use `prepare_pr_needs_changes` with `workspace_path` and `blocker_reason` for\nrecoverable PR/branch issues; this path also requires approval. Use\n`prepare_pr_needs_user_action` with `blocker_reason` for external blockers.\n\nPrepare PR emits `pr_feedback_cursor=uninitialized`; delivery preserves\nsupplied cursors and never synthesizes materialized cursors. Only the\ndeterministic PR watcher resets invalid cursors to `uninitialized` with\n`pull_request_feedback_invalid`.",
      "transition_description": "All verification passed and delivery may continue without runtime smoke.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "review_context",
          "description": "Implementation, fix, report, and artifact context required by verification.",
          "purpose": "ordinary"
        },
        {
          "key": "smoke_rationale",
          "description": "Evidence-based reason that runtime smoke is required or may be skipped.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "prepare_pr_waiting_pr",
      "source": "prepare_pr",
      "transition": "prepare_pr_monitor_ci",
      "target": "waiting_pr",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Check delivery state for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `waiting_pr`, `evidence_type` to\n`delivery`, and `context.manifest_path` to `.kent/context/delivery.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nResolved merge strategy: {{.Params.merge_strategy}}\nWorkspace: {{.Params.workspace_path}}\n\n\nUse .kent/commands/waiting-pr.md as the project procedure.\n\n\nApply the `ci-monitor` role contract. Do not merge or push. Revalidate the\nresolved method using method-specific evidence; investigate contradictory\nsource-control or user-reported feasibility without mutating the task branch.\n\nIf the PR head changed, revalidate the available checks and\nmethod in this node before starting another deterministic merge watch.\n\nChoose `waiting_pr_pr_merged` only when the source-control system conclusively reports the\nPR as merged; provide `workspace_path`, `pr_url`, `branch_name`, and\n`merge_report`. If it remains open, required checks are green, no changes are\nrequested, and the selected method is feasible, choose `waiting_pr_watch_merge` and\nprovide `workspace_path`, `pr_url`, `branch_name`, `merge_strategy`, and the\nexact current head and base commits as `pr_head_oid` and `pr_base_oid`. The\ndeterministic watcher waits without an approval or model turn and wakes this\nnode only after meaningful state changes. A base OID change wakes this node so\nmethod-specific feasibility can be revalidated through the normal merge-policy\ncontract.\n\nUse `waiting_pr_needs_user_action` only for authentication/access failure, ambiguous or\ncontradictory merge policy, or another actual human decision.\nMerely waiting for review or merge is not a blocker. Use\n`waiting_pr_needs_changes` with `workspace_path`, `merge_strategy`, and `pr_report` when\ntask code or history must change. Choose `waiting_pr_close_without_merge` only when the\nlatest user comment explicitly approves closing or canceling this PR; provide\n`workspace_path`, `pr_report`, and `closure_reason`.",
      "transition_description": "The pull request exists and its delivery state must be checked.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "prepare_pr_no_pr",
      "source": "prepare_pr",
      "transition": "prepare_pr_no_pr",
      "target": "cleanup",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Perform conservative cleanup for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nUse the current task execution root as the workspace.\nPR not applicable: {{.Params.pr_report}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nTreat cleanup as report-first. Never delete the primary checkout, dirty or\nambiguous state, or content not proven recoverable.\n\nDo not remove the managed worktree or local branch\ninside this agent session. The following deterministic Task Janitor runs only\nafter this resource-owning Cleanup session exits. It may remove an exact\ntask-owned clean managed worktree and local branch when recoverability is\nproven. For `merged`, it may also delete the same-repository remote task branch\nonly when GitHub reports the exact branch head in the merged PR and the remote\nhead has not changed.\n\nBefore `cleanup_run_janitor`, close or terminate every task-owned background shell or\nkept-open tool session, then run `kent worktree leave` from this Cleanup\nsession. Treat a failed leave request as an infrastructure blocker. The\nJanitor will refuse deletion while this session still targets the task\nworktree.\n\nComplete with `cleanup_run_janitor` and provide:\n\n- canonical `workspace_path`;\n- `task_short_id` as `{{.TaskShortId}}`;\n- `branch_name` as the exact non-empty output of\n  `git branch --show-current`, including `no_pr` and `report_only`; never use\n  `null`, `none`, `not-applicable`, an empty value, or the Kent task ID by\n  inference;\n- `pr_url` and `merge_report`, using the literal `not-applicable` when absent;\n- `cleanup_mode` as `no_pr`;\n- `cleanup_session_id` from the current `KENT_SESSION_ID`;\n- a non-empty `cleanup_report` describing preflight and preserved resources.\n\nUse `cleanup_needs_user_action` only when even conservative preservation requires a\nhuman decision.",
      "transition_description": "A pull request is not applicable; require approval before cleanup.",
      "parameters": [
        {
          "key": "pr_report",
          "description": "Pull request creation, review, conflict, or post-CI status report.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "prepare_pr_fix",
      "source": "prepare_pr",
      "transition": "prepare_pr_needs_changes",
      "target": "fix",
      "context": "compact_and_continue_session",
      "context_source": "previous_target_or_new",
      "prompt": "Resolve an approved PR or branch recovery issue.\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `fix`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}.\nRecovery issue: {{.Params.blocker_reason}}\n\nUse .kent/commands/fix.md as the project procedure.\n\n\nThe approval applies only to the exact reported PR/branch recovery. Never infer\npermission for a broader rebase or force-push. After resolving task-scoped code, complete with\n`fix_verify` and provide `workspace_path`, `task_short_id={{.TaskShortId}}`, plus\nrefreshed `review_context`.\nUse `fix_needs_user_action` with `blocker_reason` if the approved recovery is still\nunsafe. Choose `fix_wont_do` only for an explicit cancellation decision and\nprovide `closure_reason`.",
      "transition_description": "PR preparation found task-scoped changes that must be fixed.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "prepare_pr_needs_user_action",
      "source": "prepare_pr",
      "transition": "prepare_pr_needs_user_action",
      "target": "prepare_pr",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `prepare_pr` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `prepare_pr_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "fix_pr_merged_cleanup",
      "source": "fix",
      "transition": "fix_pr_merged",
      "target": "cleanup",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Perform conservative cleanup for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nWorkspace: {{.Params.workspace_path}}\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nMerge proof: {{.Params.merge_report}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nTreat cleanup as report-first. Never delete the primary checkout, dirty or\nambiguous state, or content not proven recoverable.\n\nDo not remove the managed worktree or local branch\ninside this agent session. The following deterministic Task Janitor runs only\nafter this resource-owning Cleanup session exits. It may remove an exact\ntask-owned clean managed worktree and local branch when recoverability is\nproven. For `merged`, it may also delete the same-repository remote task branch\nonly when GitHub reports the exact branch head in the merged PR and the remote\nhead has not changed.\n\nBefore `cleanup_run_janitor`, close or terminate every task-owned background shell or\nkept-open tool session, then run `kent worktree leave` from this Cleanup\nsession. Treat a failed leave request as an infrastructure blocker. The\nJanitor will refuse deletion while this session still targets the task\nworktree.\n\nComplete with `cleanup_run_janitor` and provide:\n\n- canonical `workspace_path`;\n- `task_short_id` as `{{.TaskShortId}}`;\n- `branch_name` as the exact non-empty output of\n  `git branch --show-current`, including `no_pr` and `report_only`; never use\n  `null`, `none`, `not-applicable`, an empty value, or the Kent task ID by\n  inference;\n- `pr_url` and `merge_report`, using the literal `not-applicable` when absent;\n- `cleanup_mode` as `merged`;\n- `cleanup_session_id` from the current `KENT_SESSION_ID`;\n- a non-empty `cleanup_report` describing preflight and preserved resources.\n\nUse `cleanup_needs_user_action` only when even conservative preservation requires a\nhuman decision.",
      "transition_description": "Recovery only: the pull request is already merged; skip obsolete Fix work and continue post-merge delivery.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_report",
          "description": "Proof that the pull request is merged or equivalent delivery is complete.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "waiting_pr_cleanup",
      "source": "waiting_pr",
      "transition": "waiting_pr_pr_merged",
      "target": "cleanup",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Perform conservative cleanup for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nWorkspace: {{.Params.workspace_path}}\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nMerge proof: {{.Params.merge_report}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nTreat cleanup as report-first. Never delete the primary checkout, dirty or\nambiguous state, or content not proven recoverable.\n\nDo not remove the managed worktree or local branch\ninside this agent session. The following deterministic Task Janitor runs only\nafter this resource-owning Cleanup session exits. It may remove an exact\ntask-owned clean managed worktree and local branch when recoverability is\nproven. For `merged`, it may also delete the same-repository remote task branch\nonly when GitHub reports the exact branch head in the merged PR and the remote\nhead has not changed.\n\nBefore `cleanup_run_janitor`, close or terminate every task-owned background shell or\nkept-open tool session, then run `kent worktree leave` from this Cleanup\nsession. Treat a failed leave request as an infrastructure blocker. The\nJanitor will refuse deletion while this session still targets the task\nworktree.\n\nComplete with `cleanup_run_janitor` and provide:\n\n- canonical `workspace_path`;\n- `task_short_id` as `{{.TaskShortId}}`;\n- `branch_name` as the exact non-empty output of\n  `git branch --show-current`, including `no_pr` and `report_only`; never use\n  `null`, `none`, `not-applicable`, an empty value, or the Kent task ID by\n  inference;\n- `pr_url` and `merge_report`, using the literal `not-applicable` when absent;\n- `cleanup_mode` as `merged`;\n- `cleanup_session_id` from the current `KENT_SESSION_ID`;\n- a non-empty `cleanup_report` describing preflight and preserved resources.\n\nUse `cleanup_needs_user_action` only when even conservative preservation requires a\nhuman decision.",
      "transition_description": "The pull request is confirmed merged; continue post-merge delivery.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_report",
          "description": "Proof that the pull request is merged or equivalent delivery is complete.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "waiting_pr_watch_merge",
      "source": "waiting_pr",
      "transition": "waiting_pr_watch_merge",
      "target": "merge_watch",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "The pull request is open and feasible; wait deterministically for a meaningful state change.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_head_oid",
          "description": "Pull-request head commit observed after green CI.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_base_oid",
          "description": "Pull-request base commit observed after green CI.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "merge_watch_still_waiting",
      "source": "merge_watch",
      "transition": "merge_watch_still_waiting",
      "target": "merge_watch",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "No meaningful PR state changed during a bounded watch window; continue without an agent turn.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_head_oid",
          "description": "Pull-request head commit observed after green CI.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_base_oid",
          "description": "Pull-request base commit observed after green CI.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "merge_watch_state_changed",
      "source": "merge_watch",
      "transition": "merge_watch_state_changed",
      "target": "waiting_pr",
      "context": "compact_and_continue_session",
      "context_source": "previous_target",
      "prompt": "Check delivery state for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `waiting_pr`, `evidence_type` to\n`delivery`, and `context.manifest_path` to `.kent/context/delivery.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nResolved merge strategy: {{.Params.merge_strategy}}\nWorkspace: {{.Params.workspace_path}}\n\n\nUse .kent/commands/waiting-pr.md as the project procedure.\n\n\nApply the `ci-monitor` role contract. Do not merge or push. Revalidate the\nresolved method using method-specific evidence; investigate contradictory\nsource-control or user-reported feasibility without mutating the task branch.\n\nIf the PR head changed, revalidate the available checks and\nmethod in this node before starting another deterministic merge watch.\n\nChoose `waiting_pr_pr_merged` only when the source-control system conclusively reports the\nPR as merged; provide `workspace_path`, `pr_url`, `branch_name`, and\n`merge_report`. If it remains open, required checks are green, no changes are\nrequested, and the selected method is feasible, choose `waiting_pr_watch_merge` and\nprovide `workspace_path`, `pr_url`, `branch_name`, `merge_strategy`, and the\nexact current head and base commits as `pr_head_oid` and `pr_base_oid`. The\ndeterministic watcher waits without an approval or model turn and wakes this\nnode only after meaningful state changes. A base OID change wakes this node so\nmethod-specific feasibility can be revalidated through the normal merge-policy\ncontract.\n\nUse `waiting_pr_needs_user_action` only for authentication/access failure, ambiguous or\ncontradictory merge policy, or another actual human decision.\nMerely waiting for review or merge is not a blocker. Use\n`waiting_pr_needs_changes` with `workspace_path`, `merge_strategy`, and `pr_report` when\ntask code or history must change. Choose `waiting_pr_close_without_merge` only when the\nlatest user comment explicitly approves closing or canceling this PR; provide\n`workspace_path`, `pr_report`, and `closure_reason`.\n\nThe deterministic watcher stopped because PR state changed:\n{{.Params.pr_report}}\n\n\nClassify only that fresh state. Do not repeat passive polling in the agent.",
      "transition_description": "PR state changed materially; let the retained Waiting PR session classify it once.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_report",
          "description": "Pull request creation, review, conflict, or post-CI status report.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "merge_watch_cleanup",
      "source": "merge_watch",
      "transition": "merge_watch_pr_merged",
      "target": "cleanup",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Perform conservative cleanup for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nWorkspace: {{.Params.workspace_path}}\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nMerge proof: {{.Params.merge_report}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nTreat cleanup as report-first. Never delete the primary checkout, dirty or\nambiguous state, or content not proven recoverable.\n\nDo not remove the managed worktree or local branch\ninside this agent session. The following deterministic Task Janitor runs only\nafter this resource-owning Cleanup session exits. It may remove an exact\ntask-owned clean managed worktree and local branch when recoverability is\nproven. For `merged`, it may also delete the same-repository remote task branch\nonly when GitHub reports the exact branch head in the merged PR and the remote\nhead has not changed.\n\nBefore `cleanup_run_janitor`, close or terminate every task-owned background shell or\nkept-open tool session, then run `kent worktree leave` from this Cleanup\nsession. Treat a failed leave request as an infrastructure blocker. The\nJanitor will refuse deletion while this session still targets the task\nworktree.\n\nComplete with `cleanup_run_janitor` and provide:\n\n- canonical `workspace_path`;\n- `task_short_id` as `{{.TaskShortId}}`;\n- `branch_name` as the exact non-empty output of\n  `git branch --show-current`, including `no_pr` and `report_only`; never use\n  `null`, `none`, `not-applicable`, an empty value, or the Kent task ID by\n  inference;\n- `pr_url` and `merge_report`, using the literal `not-applicable` when absent;\n- `cleanup_mode` as `merged`;\n- `cleanup_session_id` from the current `KENT_SESSION_ID`;\n- a non-empty `cleanup_report` describing preflight and preserved resources.\n\nUse `cleanup_needs_user_action` only when even conservative preservation requires a\nhuman decision.",
      "transition_description": "The deterministic watcher confirmed the PR merged.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_report",
          "description": "Proof that the pull request is merged or equivalent delivery is complete.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "waiting_pr_needs_user_action",
      "source": "waiting_pr",
      "transition": "waiting_pr_needs_user_action",
      "target": "waiting_pr",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Check delivery state for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `waiting_pr`, `evidence_type` to\n`delivery`, and `context.manifest_path` to `.kent/context/delivery.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nPR: {{.Params.pr_url}}\nBranch: {{.Params.branch_name}}\nResolved merge strategy: {{.Params.merge_strategy}}\nWorkspace: {{.Params.workspace_path}}\n\n\nUse .kent/commands/waiting-pr.md as the project procedure.\n\n\nApply the `ci-monitor` role contract. Do not merge or push. Revalidate the\nresolved method using method-specific evidence; investigate contradictory\nsource-control or user-reported feasibility without mutating the task branch.\n\nIf the PR head changed, revalidate the available checks and\nmethod in this node before starting another deterministic merge watch.\n\nChoose `waiting_pr_pr_merged` only when the source-control system conclusively reports the\nPR as merged; provide `workspace_path`, `pr_url`, `branch_name`, and\n`merge_report`. If it remains open, required checks are green, no changes are\nrequested, and the selected method is feasible, choose `waiting_pr_watch_merge` and\nprovide `workspace_path`, `pr_url`, `branch_name`, `merge_strategy`, and the\nexact current head and base commits as `pr_head_oid` and `pr_base_oid`. The\ndeterministic watcher waits without an approval or model turn and wakes this\nnode only after meaningful state changes. A base OID change wakes this node so\nmethod-specific feasibility can be revalidated through the normal merge-policy\ncontract.\n\nUse `waiting_pr_needs_user_action` only for authentication/access failure, ambiguous or\ncontradictory merge policy, or another actual human decision.\nMerely waiting for review or merge is not a blocker. Use\n`waiting_pr_needs_changes` with `workspace_path`, `merge_strategy`, and `pr_report` when\ntask code or history must change. Choose `waiting_pr_close_without_merge` only when the\nlatest user comment explicitly approves closing or canceling this PR; provide\n`workspace_path`, `pr_report`, and `closure_reason`.",
      "transition_description": "Waiting PR requires an actual human decision or external access recovery.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        },
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "waiting_pr_fix",
      "source": "waiting_pr",
      "transition": "waiting_pr_needs_changes",
      "target": "fix",
      "context": "compact_and_continue_session",
      "context_source": "previous_target_or_new",
      "prompt": "Fix task-scoped PR feedback.\n\nRead `.kent/context/implement.md` first and stay inside its required and\nconditionally triggered sources.\n\nBefore transition, pipe one non-empty JSON object to\n`.kent/scripts/workflow-evidence-ledger append --task {{.TaskShortId}} --workspace\n<workspace>`. Set `node_key` to `fix`, `evidence_type` to\n`implementation`, and `context.manifest_path` to `.kent/context/implement.md`.\n`context.files_read` lists other project instruction files in actual read\norder; do not repeat the manifest there. Record repeated questions and\nverification loops, use null for unavailable model/compaction counters, and\nexclude secrets or broad raw evidence. Append is idempotent for the current\nKent run; on recovery, reuse the returned sequence/hash and continue.\n\nWorkspace: {{.Params.workspace_path}}.\nResolved merge strategy: {{.Params.merge_strategy}}.\nPR report: {{.Params.pr_report}}\n\n\nUse .kent/commands/fix.md as the project procedure.\n\n\nRemain the single writer. Do not merge or push protected branches. A history\nrewrite or force-push requires an exact latest user authorization naming the\nbranch and permitted repair. Preserve the old remote head in a local backup,\npin the expected remote head, prove the repaired final tree is byte-for-byte\nidentical unless the authorization explicitly permits code changes, and update\nonly the task branch with force-with-lease. Stop with `fix_needs_user_action` on a\nlease, tree, target-tip, or authorization mismatch; never fall back to an\nunconditional force push.\nComplete with `fix_verify` and provide\n`workspace_path`, `task_short_id={{.TaskShortId}}`, plus refreshed\n`review_context`.\nUse `fix_needs_user_action` with `blocker_reason` for external or policy blockers.\nChoose `fix_wont_do` only for an explicit cancellation decision and provide\n`closure_reason`.",
      "transition_description": "The PR state requires task-scoped changes before merge.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_strategy",
          "description": "Resolved pull-request merge strategy: merge, squash, or rebase.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_report",
          "description": "Pull request creation, review, conflict, or post-CI status report.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "waiting_pr_close_without_merge",
      "source": "waiting_pr",
      "transition": "waiting_pr_close_without_merge",
      "target": "cleanup",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": "Perform conservative cleanup for {{.TaskShortId}}.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nWorkspace: {{.Params.workspace_path}}\nPR closure report: {{.Params.pr_report}}\nClosure reason: {{.Params.closure_reason}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nTreat cleanup as report-first. Never delete the primary checkout, dirty or\nambiguous state, or content not proven recoverable.\n\nDo not remove the managed worktree or local branch\ninside this agent session. The following deterministic Task Janitor runs only\nafter this resource-owning Cleanup session exits. It may remove an exact\ntask-owned clean managed worktree and local branch when recoverability is\nproven. For `merged`, it may also delete the same-repository remote task branch\nonly when GitHub reports the exact branch head in the merged PR and the remote\nhead has not changed.\n\nBefore `cleanup_run_janitor`, close or terminate every task-owned background shell or\nkept-open tool session, then run `kent worktree leave` from this Cleanup\nsession. Treat a failed leave request as an infrastructure blocker. The\nJanitor will refuse deletion while this session still targets the task\nworktree.\n\nComplete with `cleanup_run_janitor` and provide:\n\n- canonical `workspace_path`;\n- `task_short_id` as `{{.TaskShortId}}`;\n- `branch_name` as the exact non-empty output of\n  `git branch --show-current`, including `no_pr` and `report_only`; never use\n  `null`, `none`, `not-applicable`, an empty value, or the Kent task ID by\n  inference;\n- `pr_url` and `merge_report`, using the literal `not-applicable` when absent;\n- `cleanup_mode` as `closed_without_merge`;\n- `cleanup_session_id` from the current `KENT_SESSION_ID`;\n- a non-empty `cleanup_report` describing preflight and preserved resources.\n\nUse `cleanup_needs_user_action` only when even conservative preservation requires a\nhuman decision.",
      "transition_description": "The user explicitly approved closing delivery without merge.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_report",
          "description": "Pull request creation, review, conflict, or post-CI status report.",
          "purpose": "ordinary"
        },
        {
          "key": "closure_reason",
          "description": "User-approved reason for closing or canceling delivery without merge.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    },
    {
      "key": "cleanup_task_janitor",
      "source": "cleanup",
      "transition": "cleanup_run_janitor",
      "target": "task_janitor",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "Cleanup reporting is complete; remove only deterministically safe task-owned resources.",
      "parameters": [
        {
          "key": "workspace_path",
          "description": "Repository or managed-worktree root; never an artifact directory.",
          "purpose": "ordinary"
        },
        {
          "key": "task_short_id",
          "description": "Stable human-readable Kent task short ID.",
          "purpose": "ordinary"
        },
        {
          "key": "pr_url",
          "description": "Canonical pull request URL.",
          "purpose": "ordinary"
        },
        {
          "key": "branch_name",
          "description": "Exact non-empty current Git branch from git branch --show-current; required even when no pull request exists.",
          "purpose": "ordinary"
        },
        {
          "key": "merge_report",
          "description": "Proof that the pull request is merged or equivalent delivery is complete.",
          "purpose": "ordinary"
        },
        {
          "key": "cleanup_mode",
          "description": "Cleanup proof mode: merged, no_pr, closed_without_merge, or report_only.",
          "purpose": "ordinary"
        },
        {
          "key": "cleanup_session_id",
          "description": "Kent session ID of the completed resource-owning Cleanup node.",
          "purpose": "ordinary"
        },
        {
          "key": "cleanup_report",
          "description": "Report of cleanup performed, skipped, or blocked.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "task_janitor_done",
      "source": "task_janitor",
      "transition": "task_janitor_done",
      "target": "done",
      "context": "new_session",
      "context_source": "immediate_source",
      "prompt": null,
      "transition_description": "The janitor removed safe resources or preserved them with an explicit cleanup report.",
      "parameters": [
        {
          "key": "cleanup_report",
          "description": "Report of cleanup performed, skipped, or blocked.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "task_janitor_blocked",
      "source": "task_janitor",
      "transition": "task_janitor_blocked",
      "target": "cleanup",
      "context": "compact_and_continue_session",
      "context_source": "previous_target",
      "prompt": "Recover the task cleanup after deterministic Janitor failure.\n\nRead `.kent/context/delivery.md` first and stay inside its required and\nconditionally triggered sources.\n\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.\n\nPrevious cleanup report:\n{{.Params.cleanup_report}}\nBlocker:\n{{.Params.blocker_reason}}\n\nUse .kent/commands/cleanup-task.md as the project procedure.\n\nUse the retained Cleanup context. Do not directly remove a Kent-managed\nworktree from this agent session. Close every task-owned background shell or\nkept-open tool session. If this session still targets the task worktree, run\n`kent worktree leave` before retrying. If the infrastructure failure is\ntransient and the same safety proofs still hold, choose `cleanup_run_janitor` again\nwith the complete canonical parameter contract. Otherwise choose\n`cleanup_needs_user_action` with the exact blocker. Preserve every ambiguous or unique\nresource.",
      "transition_description": "Janitor infrastructure failed after safety checks; return to Cleanup without losing its context.",
      "parameters": [
        {
          "key": "cleanup_report",
          "description": "Report of cleanup performed, skipped, or blocked.",
          "purpose": "ordinary"
        },
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": false
    },
    {
      "key": "cleanup_needs_user_action",
      "source": "cleanup",
      "transition": "cleanup_needs_user_action",
      "target": "cleanup",
      "context": "compact_and_continue_session",
      "context_source": "immediate_source",
      "prompt": "Resume the `cleanup` stage after user action.\n\nPrevious blocker: {{.Params.blocker_reason}}\n\nUse the retained compacted context and re-read current task comments and\nproject instructions. Approval means only that the exact reported blocker\naction is now complete; it is not acknowledgement that waiting may begin.\nVerify resolution before continuing. If the blocker remains, preserve task\nscope and return `cleanup_needs_user_action` again. Do not infer approval for any\nbroader or destructive action.\nTerminal Cleanup evidence has one project-owned owner: `.kent/scripts/workflow-prepare-cleanup`.\nInvoke it in the original Task root using the project Cleanup procedure.\nUse .kent/commands/cleanup-task.md as the project procedure.\nThe project command alone owns the final ordinary append and terminal seal.\nPreserve actual context metrics through its project-owned final-event contract;\nkeep its payload and archive protocol in the project procedure.\n\nDo not issue a standalone ledger append before the helper, after seal, or\nmerely to report a blocker. Until valid terminal preparation is possible,\nkeep preflight and blocker observations in retained Task records.\nUse real current, unmodified `KENT_SESSION_ID`, `KENT_RUN_ID` and `KENT_STEP_ID`.\nNever generate, export, substitute or replace identities to bypass deduplication.\nIf conflicting or fabricated evidence or substituted identities are present,\npreserve the evidence and block. Do not accept that seal as valid, overwrite\nits history, reconstruct it or discard-and-reseal.\n\nOnly genuinely successful prior preparation may use the existing project-\nvalidated frozen-request/report recovery, without another ordinary append.\nFreeze the original root, branch and validated report before leaving the\nTask worktree. Do not call relative project adapters from pinned primary\nafter leave or re-read its branch as the Task branch.",
      "transition_description": "Work is externally blocked. Do not approve until the reported external action is complete; approval resumes this stage.",
      "parameters": [
        {
          "key": "blocker_reason",
          "description": "Exact blocker and the next user or external action required.",
          "purpose": "ordinary"
        }
      ],
      "assignee_selection": "configured",
      "thinking_selection": "configured",
      "requires_approval": true
    }
  ]
}

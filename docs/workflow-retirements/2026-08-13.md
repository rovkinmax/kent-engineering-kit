# Workflow Retirements — 2026-08-13

The operator approved validating compatible workflows in place and retiring
completed Canary and obsolete revisions. Live deletion is performed only
after a fresh Kent deletion preview matches this record.

Deleted workflow task history is intentionally disposable. A retained JSON
snapshot can reconstruct an equivalent graph under a new workflow UUID, but
cannot restore task IDs, comments, approvals, sessions, or project links.

## Kent 2.6.1 Validation

Every workflow linked to AppsomeAndroid, Puber, osome-kmp-mobile-shared, and
osome-slack-reader was inspected as a complete graph, reapplied without
confirmation, and returned `unchanged` without incrementing its workflow
version. Every retained workflow then passed execution validation.

This establishes transport compatibility without inventing duplicate workflow
revisions. Task-backed graphs remain frozen; any future semantic delta still
requires a new revision.

## Approved Retirement Set

- Appsome Engineering Canary v25
  (`ce02c66b-3c7e-4e55-b5c0-654a25c83057`), including completed task
  `OSM-65`.
- Puber Engineering Canary v20
  (`b88414ed-ece8-4a08-91ad-cec538eaf320`), including completed task
  `PUB-65`.
- Osome SDK Engineering Canary v12
  (`2bab615c-7dd3-473b-a242-a0938008bd30`), including completed task
  `OSDK-9`.
- Osome Slack Reader Engineering Canary v3
  (`bb64b91b-6eab-436c-adb2-16ec8d4fe718`), including completed task
  `OSR-10`.
- Osome SDK Engineering Delivery v11
  (`9378aa53-efbe-44f1-8526-dee1c8388c4f`), with no tasks.
- Osome Slack Reader Engineering Delivery v2
  (`49584b67-203d-4254-9226-087ee418fb20`), with no tasks.
- Puber Engineering Delivery v19
  (`ffa260fe-ff11-40f2-8611-8547be06c8fe`), including obsolete Backlog
  records `PUB-61` and `PUB-62`.
- Unlinked taskless Appsome Release Publication v2 candidate
  (`f0dcad4e-c6a9-4822-820e-ca3d0bba8a67`).

## Puber Backlog Replacement

- `PUB-61` → `PUB-63`
  - title, body, source URL, source workspace, and labels match;
  - body SHA-256:
    `3718ca7afc98a5b3a61715501936a4173de05a19f969ecfed7361effcea3f970`.
- `PUB-62` → `PUB-64`
  - title, body, empty source URL, source workspace, and labels match;
  - body SHA-256:
    `acf9997974da8224443fed9e3df5de68d1c5d8247f601f326489c0be3997cb9b`.

The replacement tasks remain in Backlog on Puber Engineering Delivery v20.
The old records were never started and had no retained sessions or
dependencies.

## Appsome Backlog Replacement

- `OSM-62` → `OSM-66`
  - title, body, source URL, source workspace, and labels match.
- `OSM-63` → `OSM-67`
  - title, body, Jira source URL, source workspace, and labels match;
  - the prior OSM-54 → OSM-63 migration comment was copied to OSM-67.

The replacement tasks remain in Backlog on Appsome Engineering Delivery v25.
The old records remain frozen on v24 and carry do-not-start comments until
OSM-64 finishes and v24 can be retired.

## Preserved Revisions

- Appsome Engineering Delivery v21 remains linked because `OSM-59` is running.
- Appsome Engineering Delivery v24 remains linked because `OSM-64` is waiting
  for approval and `OSM-62`/`OSM-63` remain in Backlog.
- Current Delivery defaults and all specialized linked workflows remain.

## Cleanup

The four Canary managed worktrees were absent before retirement. The local
SDK branch `OSDK-9` matched `main`, had no remote branch or worktree, and was
eligible for safe `git branch -d` deletion.

No Kent configuration changed, so no service or Desktop restart was required.

## Completed Deletion Impacts

- Appsome Canary v25: one project link and completed task `OSM-65`.
- Puber Canary v20: one project link and completed task `PUB-65`.
- SDK Canary v12: one project link and completed task `OSDK-9`.
- Slack Reader Canary v3: one project link and completed task `OSR-10`.
- SDK Delivery v11: one project link and no tasks.
- Slack Reader Delivery v2: one project link and no tasks.
- Puber Delivery v19: one project link and obsolete Backlog tasks `PUB-61` and
  `PUB-62`.
- Unused Appsome Release Publication v2: no project links and no tasks.

Every confirmed preview reported zero current nodes, pending approvals, blocked
tasks, and default replacements.
